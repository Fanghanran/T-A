# better-sqlite3 预编译包下载走 npmmirror 镜像：
# GitHub Releases 直连在本机网络下会 ECONNRESET，导致回退 node-gyp 源码编译失败（缺 MSVC）。
# 仅影响二进制包下载源，不改变 registry。
better-sqlite3-binary-host-mirror=https://npmmirror.com/mirrors/better-sqlite3
