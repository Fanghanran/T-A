## 目标
把本次整理出的《统一实施路线图》落盘为一份文档，**不写任何功能代码、不改任何 .js/.jsx/配置/lockfile**。

## 唯一交付物
新增 `docs/ROADMAP.md`（若你更希望放 `docs/adr/000-roadmap.md` 可在批准时说一句），内容为本会话已确认的规划，结构：

1. **现状基线**（已完成并实测）：分层+check-layers、agent registry、Milvus 迁移、切片写耐久(ADR-004)、认证/CORS/限流/会话绑定、质量门禁+CI、最近一轮债务清偿。
2. **关键发现（需正视）**：
   - 项目**未纳入 git**（无 `.git`），`ci.yml`/回滚实际失效 → 列 M0。
   - 文档漂移：`ARCHITECTURE.md` CI 状态矛盾、`docs/` 仍引用 Vectra、三套设计未落盘。
3. **三份未落盘设计（登记为待写 ADR，本次不写实现，只在 ROADMAP 里摘要）**：
   - 模型管理（多模型路由·运行时热改·三级选择·Embedding 纳入含重建）→ 未来 ADR-006。
   - 会话记忆（两层：会话内滚动摘要 + 跨会话事实向量库）→ 未来 ADR-007。
   - 多智能体并行 / 多用户 → 未来 ADR-008。
   - 摘要 + 指向决策（沿用你已选：模型管理“管理页热改/三级/Embedding 完整”；记忆“两层分阶段/LLM提炼向量化”）。
4. **里程碑与依赖顺序**：M0 工程地基(git init+文档校准) → M0.5 落盘 006/007/008 ADR → M1 模型管理 → M2 记忆 / M4 并行(依赖 M1) → M3 残留债务(并行) → M5 多用户/多实例(暂缓·条件性)。给出依赖链与关键约束（换 embedding=破坏性重建、进程内资源多实例前需外置、单用户零回归）。
5. **明确不做 + 环境约束**（Windows 无 C++ Build Tools、Milvus OOM 史等）。
6. **每里程碑的验证方式**（`check:all`、新增 ADR、必要单测），但标注“本次均不执行”。

## 严格不做
- 不 `git init`、不改代码/配置/依赖/lockfile、不新增 `server/lib`、`src/components` 等实现文件。
- 不写 M0.5 的 ADR 正文（本 ROADMAP 只做“摘要+待办登记”，正式 ADR 留到各自实施里程碑）。

## 批准后的动作
仅创建 `docs/ROADMAP.md`，然后停下等你后续指令（如“开工 M0”）。